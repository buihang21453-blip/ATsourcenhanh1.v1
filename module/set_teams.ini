; PES Arena AT Set Team Core v1.5.0
; Core-hook mode: AT changes team_id_encoded inside the original PES set_team_id flow.
; IDs are decimal PES team IDs.

[set_teams]
enabled = 1
home = 107
away = 101
